@model IEnumerable<XavSpace.Website.ViewModels.Boards.NoticeBoardIndexViewModel>

@{
    Layout = null;
}

@foreach (var item in Model)
{
    @Html.Partial("_BoardsPartial", item)
}

@if (Model.Count() == 0)
{
    <div class="xs-done">No more Notice Boards.</div>
}